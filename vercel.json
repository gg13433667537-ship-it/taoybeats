{
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy && prisma generate && NEXT_PUBLIC_VERSION=$(node -p \"require('./package.json').version\") NEXT_PUBLIC_COMMIT=$VERCEL_GIT_COMMIT_SHA npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --legacy-peer-deps",
  "regions": ["hnd1"]
}
